#include<stdio.h>
#include<stdio.h>
void main()
{
    int a[100],n,i,j,min,temp;
    arscr();
     printf("\n enter the number of elements:");
     scanf("\d",&n);
    printf("\n enter %d elements:',n);
    for(i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }
           for(i=0;i<n-1; i++)
           {
              min=i;
              for(j=i+1;j++)
           }
           {
              if(a[min]>a[j])
              min=j;
           }
           if(min=i)
           {
              temp=a[i];
              a[i]=a[mid];
              a[min]=temp;
           }
           printf("\")n the sorted array in ascending order
           for(i=0;i<n;i++)
           {
              printf("%d",a[i]);
           }
              getch();
}
