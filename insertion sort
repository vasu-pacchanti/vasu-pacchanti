#include<stdio.h>
void insertion sort(int arr[],intn);
int main(){
int arr[]={5,2,9,1,5,6}
int n=size of (arr)/size of (arr[0]);
printf(origoinal array:");
for (int i=0; i<n; i++){
printf("\n sorted array:");
for(int i=0,i<n;i++);{
printf("%d",arr[i]);
}
return 0;
}
