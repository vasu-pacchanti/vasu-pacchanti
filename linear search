#include<stdio.>
int main()
{
    int array[100],search,count,number;
    prinntf("enter the number of elements in array/n");
    scanf("%d",& number);
    printf("enter %d number/n",number)
    for(count=0; count<number;coount++)
    scanf("%d",& array[count]);
    printf("enter the number of search/n");
    scanf("%d",&search);
    for (count=0; count<number; count++)
    {
        if (array[count]==search)
    {
        printf("%d is present at location %d"/n search,counnt+1);
        break;
    }
    }
    if(count==number)
    printf("%d is not present in array.\n",search);
    return 0;
}
